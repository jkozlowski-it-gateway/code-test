import { useState } from "react";

// TASK 3 call 'testFunc' function every time 'testState' changes it's value. Make sure function is called AFTER the state change

function Task3() {
  const [testState, setTestState] = useState(false);

  const testFunc = () => {
    //
  };

  return null;
}

export default Task3;
