import { useState } from "react";

// TASK 4 using Fetch API call 'FAKE_ENDPOINT' (GET method) with 'ACCESS_TOKEN' as Bearer Authentication and save the response to 'testState'

const FAKE_ENDPOINT = "http://example.com/path";

const ACCESS_TOKEN = "test_token";

function Task4() {
  const [testState, setTestState] = useState(null);

  const testFunc = () => {
    // CODE HERE
  };

  return null;
}

export default Task4;
